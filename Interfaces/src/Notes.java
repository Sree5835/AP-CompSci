
public class Notes {
	//a list of method headers and constants
	//returnType methodName (parameters);
	//don't have instance fields, constructors, method bodies.
	//any class that implements an interface must "implement" (code) all the methods listed in the interface.
}
